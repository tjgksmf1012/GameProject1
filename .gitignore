.godot/
*.tmp

# export_presets.cfg 는 **일부러 저장소에 넣는다.** Godot 기본 .gitignore 가 이걸 빼는 건
# 서명 키 경로·비밀번호가 들어갈 수 있기 때문인데, 이 프로젝트 프리셋에는 그런 게 없다
# (codesign 꺼짐, 키스토어 없음). 빼두면 빌드가 재현되지 않고, 에디터에서만 존재하는
# 설정이 되어 CLAUDE.md 1.2 를 정면으로 어긴다 — 내가 고칠 수 없는 설정이 생긴다.
# 나중에 서명을 붙이게 되면 그 값만 환경변수로 빼고 이 파일은 그대로 둔다.

# 내보낸 빌드
/build/
